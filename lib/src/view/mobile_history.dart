import 'package:flutter/material.dart';

class MobileHistory extends StatelessWidget {
  const MobileHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("최근 본 글이 없습니다."),
    ));
  }
}
